{% set name = "pomegranate" %}
{% set version = "0.13.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/jmschrei/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 16ffa0b835007e33432ae08ce79a4da26c5ed32afbb38f5356b27c0e143a9aa5

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - cython >=0.22.1
    - numpy >=1.8.0
    - pip
    - python
    - scipy >=0.17.0
  run:
    - joblib >=0.9.0b4
    - networkx >=2.0
    - {{ pin_compatible("numpy") }}
    - python
    - pyyaml
    - scipy >=0.17.0

test:
  imports:
    - pomegranate
    - pomegranate.distributions

about:
  home: http://pypi.python.org/pypi/pomegranate/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Pomegranate is a graphical models library for Python, implemented in Cython for speed.
  doc_url: https://pomegranate.readthedocs.io
  dev_url: https://github.com/jmschrei/pomegranate

extra:
  recipe-maintainers:
    - micknudsen
